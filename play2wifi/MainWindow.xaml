<Window x:Class="play2wifi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:si="clr-namespace:Expression.Samples.Interactivity;assembly=Expression.Samples.Interactivity"
        Title="MainWindow" 
        Visibility="Hidden" 
        Name="mainWindow">
    <Grid>
        <tb:TaskbarIcon Name="taskbarIcon" ToolTipText="play2wifi - AirPlay for Windows">
            <tb:TaskbarIcon.TrayPopup>
                <Border
      Background="White"
      BorderBrush="Orange"
      BorderThickness="2"
      CornerRadius="4"
      Width="160"
      Height="40">
                    <Button
        Content="Exit"
        HorizontalAlignment="Center"
        VerticalAlignment="Center" Click="Button_Click">
                        <!--<i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <si:CallMethod MethodName="Close" TargetName="mainWindow" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>-->
                    </Button>
                </Border>
            </tb:TaskbarIcon.TrayPopup>
        </tb:TaskbarIcon>
    </Grid>
</Window>
